---
import list from '@/content/recently-read/list.yml';

type Props = {
  top?: number;
};

const { top = 5 } = Astro.props;
---

<aside class="aside">
  <h2 class="heading">Recently read</h2>

  <ul class="list">
    {
      list.slice(0, top).map((item) => (
        <li>
          <a href={item.url}>{item.title ?? item.url}</a>
        </li>
      ))
    }
  </ul>
</aside>

<style>
  .aside {
    background-color: rgb(var(--background-accent-color));
    margin-block: clamp(1rem, 2.5vw, 2rem);
    margin-inline: calc(-1 * var(--padding));
    padding: var(--padding);

    @media (min-width: 48rem) {
      border-radius: 0.5rem;
      column-gap: 4rem;
      display: grid;
      grid-template-columns: 8rem 1fr;
    }
  }

  .heading {
    font-size: var(--font-size-base);
    font-weight: 600;
    margin-block-start: 0;

    @media (min-width: 48rem) {
      font-weight: 400;
    }
  }

  .list {
    display: flex;
    flex-direction: column;
    gap: 1rem;
    list-style-type: none;
    margin-block: 0;
    padding-inline-start: 0;
  }
</style>
